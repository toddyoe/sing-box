### 结构

```json
{
  "enabled": true,
  "disable_sni": false,
  "server_name": "example.com",
  "insecure": false
}
```

### 字段

`enabled` `disable_sni` `server_name` `insecure` 详情参阅 [TLS 字段](/zh/configuration/shared/tls/#outbound)。